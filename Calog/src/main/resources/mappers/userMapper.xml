<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.calog.model.dao.UserDao">
    <resultMap id="UserResultMap" type="com.calog.model.dto.User">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="userPw" column="user_pw" />
        <result property="nickname" column="nickname" />
    </resultMap>

    <select id="selectAllUser" resultMap="UserResultMap">
        SELECT *
        FROM user
    </select>
    <insert id="insertUser" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (user_id,user_pw,nickname)
        VALUES (#{userId}, #{userPw},#{nickname})
    </insert>

    <select id="findById" resultType="User" parameterType="String">
        SELECT *
        FROM user
        WHERE user_id = #{userId};
    </select>


</mapper>